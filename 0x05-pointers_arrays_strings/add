#include <stdio.h>

/**
 * _puts - prints a string, followed by a new line, to stdout.
 * @str: string to print.
 *
 * Return: void.
 */
void _puts(char *str)
{
        int i;

        for (i = 0; str[i] != '\0'; i++)
        {
                putchar(str[i]);
        }
        putchar('\n');
}
The only change made is the inclusion of the stdio.h header at the top of the file. This allows the putchar function to be used in the code.





t 
